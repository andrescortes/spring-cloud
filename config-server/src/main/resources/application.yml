spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
#          uri: file:///C:/Users/andre/Documents/courses/backend/fh/config-server
          uri: https://github.com/andrescortes/config-server

server:
  port: ${APP_PORT:8888}
